{
  "name": "aiseparations-social-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Daily social content agent for AI Separations (aiseparations.com). Drafts a screen-print/DTF post via Claude, generates an image (gpt-image-1) or reuses a promo clip, and publishes to Instagram/TikTok/Facebook/etc. via Upload-Post.",
  "scripts": {
    "post": "tsx src/index.ts",
    "post:dry": "tsx src/index.ts --dry-run",
    "post:image": "tsx src/index.ts --force-image",
    "post:video": "tsx src/index.ts --force-video",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "dotenv": "^16.4.5",
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": { "node": ">=20" }
}
